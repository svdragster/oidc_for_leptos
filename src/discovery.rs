// Optional OIDC discovery (.well-known/openid-configuration).
// Low priority — static endpoint config works for most use cases.
// Can be implemented later when needed.

// Placeholder for future implementation:
// pub async fn fetch_discovery(issuer: &str) -> Result<DiscoveryDocument, OidcError> { ... }
